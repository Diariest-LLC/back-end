package com.diariest.server.task;

import com.diariest.server.Main;

import java.util.HashMap;
import java.util.concurrent.TimeUnit;

public class TaskCores {

    /*public static HashMap<String, SolucionTask> RUNNABLE_LIST = new HashMap<String, SolucionTask>();
    public static String create(SolucionCallable callable, long initialDelay, long delay, TimeUnit timeUnit) {
        String key = UtilUUID.createUUID();
        Main.getExecutorService().schedule(callable, delay, timeUnit);
        return key;
    }*/

}
